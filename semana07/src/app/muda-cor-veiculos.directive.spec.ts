import { MudaCorVeiculosDirective } from './muda-cor-veiculos.directive';

describe('MudaCorVeiculosDirective', () => {
  it('should create an instance', () => {
    const directive = new MudaCorVeiculosDirective();
    expect(directive).toBeTruthy();
  });
});
